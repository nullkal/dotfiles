#!/bin/sh

cat <<'EOT' > ~/.dotfiles.conf
# ~/.dotfiles.conf
# ----
# This file is automatically generated by dotfiles scripts.
# 

set -a
DOTFILES_CONFIG_LOADED=1
EOT

basedir=`dirname $0`
for file in `find $basedir/tests -type f`; do
	$file
done
